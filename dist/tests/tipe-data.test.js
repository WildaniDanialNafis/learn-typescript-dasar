"use strict";
describe('Data type', function () {
    it('Should must declare', function () {
        let name = "Wildani Danial Nafis";
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 100;
        // balance = "Wildani";
        // isVip = 100;
    });
});
