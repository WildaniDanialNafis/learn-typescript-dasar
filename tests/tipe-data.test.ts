describe('Data type', function () {
    it('Should must declare', function () {
        let name: string = "Wildani Danial Nafis";
        let balance:number = 1000000;
        let isVip:boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);

        // name = 100;
        // balance = "Wildani";
        // isVip = 100;
    });
});